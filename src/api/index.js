/*
import axios from "axios";

const api = {
    /!*
    * login.js
    *!/
    loginApi(data) {
        return axios({
            'url': '/employee/login',
            'method': 'post',
            data
        })
    },
    logoutApi() {
        return axios({
            'url': '/employee/logout',
            'method': 'post',
        })
    }
}

export default api*/
